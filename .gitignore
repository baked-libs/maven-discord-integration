# Packages will be installed on GitHub, thus they do not need
# to be present in the repository
/node_modules

# The dist folder will be generated by the CI process, so we don't
# need the local folder being uploaded
dist
