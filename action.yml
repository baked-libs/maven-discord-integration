name: 'classic-discord-webhooks'
description: 'This GitHub Action sends  fancy and more meaningful discord messages for your commits.'

inputs:
  id:
    description: The Discord Webhook id
    required: true
  token:
    description: The Discord Webhook token
    required: true

runs:
  using: 'node12'
  main: 'src/index.js'
  
branding:
  icon: check-circle
  color: black
